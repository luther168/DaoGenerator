package cn.luo.sortout.model.dao;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import cn.luo.sortout.model.entity.PlanAndFolder;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "plan_and_folder".
*/
public class PlanAndFolderDao extends AbstractDao<PlanAndFolder, Long> {

    public static final String TABLENAME = "plan_and_folder";

    /**
     * Properties of entity PlanAndFolder.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "id");
        public final static Property PlanId = new Property(1, long.class, "planId", false, "plan_id");
        public final static Property FolderId = new Property(2, long.class, "folderId", false, "folder_id");
    }


    public PlanAndFolderDao(DaoConfig config) {
        super(config);
    }
    
    public PlanAndFolderDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"plan_and_folder\" (" + //
                "\"id\" INTEGER PRIMARY KEY ," + // 0: id
                "\"plan_id\" INTEGER NOT NULL ," + // 1: planId
                "\"folder_id\" INTEGER NOT NULL );"); // 2: folderId
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"plan_and_folder\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, PlanAndFolder entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindLong(2, entity.getPlanId());
        stmt.bindLong(3, entity.getFolderId());
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, PlanAndFolder entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindLong(2, entity.getPlanId());
        stmt.bindLong(3, entity.getFolderId());
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public PlanAndFolder readEntity(Cursor cursor, int offset) {
        PlanAndFolder entity = new PlanAndFolder( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.getLong(offset + 1), // planId
            cursor.getLong(offset + 2) // folderId
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, PlanAndFolder entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setPlanId(cursor.getLong(offset + 1));
        entity.setFolderId(cursor.getLong(offset + 2));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(PlanAndFolder entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(PlanAndFolder entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(PlanAndFolder entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
