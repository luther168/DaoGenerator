package com.cn.luo.helper.distribution.model.dao;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.cn.luo.helper.distribution.model.entity.PlanAndSuffix;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "plan_and_suffix".
*/
public class PlanAndSuffixDao extends AbstractDao<PlanAndSuffix, Long> {

    public static final String TABLENAME = "plan_and_suffix";

    /**
     * Properties of entity PlanAndSuffix.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "id");
        public final static Property PlanId = new Property(1, long.class, "planId", false, "plan_id");
        public final static Property SuffixId = new Property(2, long.class, "suffixId", false, "suffix_id");
    }


    public PlanAndSuffixDao(DaoConfig config) {
        super(config);
    }
    
    public PlanAndSuffixDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"plan_and_suffix\" (" + //
                "\"id\" INTEGER PRIMARY KEY ," + // 0: id
                "\"plan_id\" INTEGER NOT NULL ," + // 1: planId
                "\"suffix_id\" INTEGER NOT NULL );"); // 2: suffixId
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"plan_and_suffix\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, PlanAndSuffix entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindLong(2, entity.getPlanId());
        stmt.bindLong(3, entity.getSuffixId());
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, PlanAndSuffix entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindLong(2, entity.getPlanId());
        stmt.bindLong(3, entity.getSuffixId());
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public PlanAndSuffix readEntity(Cursor cursor, int offset) {
        PlanAndSuffix entity = new PlanAndSuffix( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.getLong(offset + 1), // planId
            cursor.getLong(offset + 2) // suffixId
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, PlanAndSuffix entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setPlanId(cursor.getLong(offset + 1));
        entity.setSuffixId(cursor.getLong(offset + 2));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(PlanAndSuffix entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(PlanAndSuffix entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(PlanAndSuffix entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
