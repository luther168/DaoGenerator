package com.cn.luo.helper.distribution.model.dao;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.cn.luo.helper.distribution.model.entity.FolderAndSuffix;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "folder_and_suffix".
*/
public class FolderAndSuffixDao extends AbstractDao<FolderAndSuffix, Long> {

    public static final String TABLENAME = "folder_and_suffix";

    /**
     * Properties of entity FolderAndSuffix.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "id");
        public final static Property FolderId = new Property(1, long.class, "folderId", false, "folder_id");
        public final static Property SuffixId = new Property(2, long.class, "suffixId", false, "suffix_id");
    }


    public FolderAndSuffixDao(DaoConfig config) {
        super(config);
    }
    
    public FolderAndSuffixDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"folder_and_suffix\" (" + //
                "\"id\" INTEGER PRIMARY KEY ," + // 0: id
                "\"folder_id\" INTEGER NOT NULL ," + // 1: folderId
                "\"suffix_id\" INTEGER NOT NULL );"); // 2: suffixId
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"folder_and_suffix\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, FolderAndSuffix entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindLong(2, entity.getFolderId());
        stmt.bindLong(3, entity.getSuffixId());
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, FolderAndSuffix entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindLong(2, entity.getFolderId());
        stmt.bindLong(3, entity.getSuffixId());
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public FolderAndSuffix readEntity(Cursor cursor, int offset) {
        FolderAndSuffix entity = new FolderAndSuffix( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.getLong(offset + 1), // folderId
            cursor.getLong(offset + 2) // suffixId
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, FolderAndSuffix entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setFolderId(cursor.getLong(offset + 1));
        entity.setSuffixId(cursor.getLong(offset + 2));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(FolderAndSuffix entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(FolderAndSuffix entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(FolderAndSuffix entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
